#include <stdio.h>
void main() {
    int n,even,odd;
    printf("Enter the number n: ");
    scanf("%d", &n);

    if (n % 2 == 0) {
        printf("%d",even);
    } else {
        printf("%d",odd);
    }
}
